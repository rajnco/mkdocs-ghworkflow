[project]
name = "mkdocs-ghworkflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mkdocs-material>=9.7.7",
]

[dependency-groups]
dev = [
    "ruff>=0.16.8",
]
